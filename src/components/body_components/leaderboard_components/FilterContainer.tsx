import { styled } from "styled-components";

const StyledFilterContainer = styled.div`
  padding: 24px;
  background: #0a2740;
  margin-top: 12px;
`;
const FilterContainer = () => {
  return <StyledFilterContainer></StyledFilterContainer>;
};

export default FilterContainer;
