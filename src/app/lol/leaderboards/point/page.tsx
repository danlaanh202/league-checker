"use client";
import { styled } from "styled-components";

const StyledLeaderboardContainer = styled.div``;
const Point = () => {
  return <StyledLeaderboardContainer></StyledLeaderboardContainer>;
};

export default Point;
